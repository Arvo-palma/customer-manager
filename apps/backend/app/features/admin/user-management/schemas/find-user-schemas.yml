components:
  schemas:
    FindUserOutput:
      $ref: '#components/schemas/userModel'
  parameters:
    FinduserId:
      name: userId
      in: path
      type: string
      required: true
