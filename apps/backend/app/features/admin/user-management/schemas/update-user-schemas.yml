components:
  schemas:
    UpdateUserInput:
      properties:
        name:
          type: string
        email:
          type: string
        role:
          type: string
        avatar:
          type: string
        password:
          type: string
        temporaryPassword:
          type: string
    UpdateUserOutput:
      $ref: '#components/schemas/UserModel'
  parameters:
    UpdateUserId:
      name: userId
      in: path
      type: string
      required: true
